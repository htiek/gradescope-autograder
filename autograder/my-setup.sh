#!/bin/bash
#
# Custom setup script to execute when setting up the GradeScope autograder.
# Feel free to include any setup you need to do after the autograder is
# uploaded to the server. The most common example would be using apt-get to
# install packages that are needed beyond what we'll normally install.
